# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'testForTeshbase' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for testForTeshbase
  	pod 'Kingfisher'
  	pod 'Lightbox'
  	pod 'Alamofire', '~> 5.2'
  	pod 'SnapKit'

  target 'testForTeshbaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'testForTeshbaseUITests' do
    # Pods for testing
  end

end
